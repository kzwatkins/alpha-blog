<h1 align="center">Welcome to <%= @user.username %>'s Page!</h1>
<div class="row">
	<div class="col-md-4 col-md-offset-4 center">
		<%= gravatar_for @user, size: 150 %>
	</div>
</div>

<h4 align="center">Welcome to <%= @user.username %>'s Articles!</h4>

<% if @user_articles %>
<div align="center">
	<%= will_paginate @user_articles %>
</div>

	<%= render "articles/article", objs: @user_articles %>

<div align="center">
	<%= will_paginate @user_articles %>
</div>

<% end %>
